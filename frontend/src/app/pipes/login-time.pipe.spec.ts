import { LoginTimePipe } from './login-time.pipe';

describe('LoginTimePipe', () => {
  it('create an instance', () => {
    const pipe = new LoginTimePipe();
    expect(pipe).toBeTruthy();
  });
});
